---
// Stub — full implementation in Task 15
interface Props {
  preInput: { activity_id: string; author: string; note: string; url?: string; }[];
  suggestMode: boolean;
  tripSlug: string;
}
const { preInput, suggestMode, tripSlug } = Astro.props;
---
<!-- SidePanel stub -->
